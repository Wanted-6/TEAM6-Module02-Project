spring:
  application:
    name: project-module2-lms

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/meat4
    username: wanted
    password: wanted

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update

    
    properties:
      hibernate:
        format_sql: true