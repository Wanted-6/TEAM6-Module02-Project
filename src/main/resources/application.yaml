spring:
  application:
    name: projectmodule2lms

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/meatdb
    username: meat4
    password: meat4

  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update

    
    properties:
      hibernate:
        format_sql: true